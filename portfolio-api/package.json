{
  "name": "portfolio-api",
  "version": "1.0.0",
  "description": "A simple portfolio API that uses access tokens with both low-privilege and high-privilege scopes",
  "author": "Curity AB",
  "main": "src/index.ts",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
      "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file=.env --import=tsx --watch ./src/index.ts",
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jose": "^6.1.3",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
