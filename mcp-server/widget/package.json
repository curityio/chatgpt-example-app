{
  "name": "portfolio-widget",
  "version": "1.0.0",
  "description": "A ChatGPT app that uses step-up authentication",
  "author": "Curity AB",
  "license": "Apache-2.0",
  "main": "src/portfolio.tsx",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "watch": "mkdir -p dist && cp app.css dist && esbuild src/portfolio.tsx --bundle --outfile=dist/bundle.js --loader:.tsx=tsx --loader:.ts=tsx --watch",
    "build": "mkdir -p dist && cp app.css dist && esbuild src/portfolio.tsx --bundle --outfile=dist/bundle.js --loader:.tsx=tsx --loader:.ts=tsx --minify"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  }
}
